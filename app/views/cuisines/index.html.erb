<h2>Type of Cuisines</h2>
<%= search_form_for @q do |f| %>

  <%= f.search_field :name_cont, :placeholder => 'Type Cuisine', :autofocus => 'true' %>

  <%= f.submit %>
<% end %>
<% @cuisines.each do |c| %>
<p>
  <%= link_to c.name, cuisine_path(c.id)%>
</p>
<% end %>
